# Non-secret defaults loaded by LangGraph CLI inside the production container.
# Secrets and model endpoints are injected as process environment variables.
LANGSMITH_TRACING=false
LANGCHAIN_TRACING_V2=false
